python-docx==1.1.2
python-pptx==1.0.2
loguru==0.7.3
pypinyin==0.55.0
cryptography==43.0.3
requests==2.32.3
Flask==3.1.0
netifaces==0.11.0
openpyxl==3.1.5
Pillow==11.1.0
pystray==0.19.5
pywin32==308
tkinterdnd2==0.4.2
ttkbootstrap==1.10.1
user_agents==2.2.0
cheroot==10.0.1
Werkzeug==3.1.3
pyotp==2.9.0

# 开发/测试依赖
pytest==8.3.5

# 打包依赖（Windows 服务可用性关键！）
# 必须使用 PyInstaller 4.10：其 onefile 可直接作为 Windows 服务镜像（子进程可连接 SCM）。
# PyInstaller 5.1+ 的 onefile 改为父/子双进程，子进程无法连接服务控制器，
# 服务启动即失败（1063/1067）。构建命令：
#   venv_3.8\Scripts\pyinstaller main-onefile.spec --noconfirm
pyinstaller==4.10
pyinstaller-hooks-contrib==2022.15

# WebDAV（可选功能，默认关闭；独立端口 8081）
wsgidav==4.3.5
defusedxml==0.7.1
py-ip2region==3.0.4
